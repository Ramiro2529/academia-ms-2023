spring:
  application:
    name: items
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/prueba?useSSL=false&serverTimezone=America/Mexico_City&useLegacyDatetimeCode=false
    username: root
    password: admin
    hikari:
      connection-test-query: SELECT 1
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    open-in-view: false


